.nav-container.c(ng-class='getBackground()')
	accordion(close-others='true')
		accordion-group(ng-repeat='c in col', is-open='c.isOpen')
			accordion-heading
				div
					{{c.name}}
			ul.list-group
				li.list-group-item(ng-repeat='i in c.items', ng-class='getColor()')
					span {{i.name}}